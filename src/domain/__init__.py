"""Domain layer — pure business logic, no I/O, no external dependencies.

This layer defines value objects, error types, and pipeline abstractions
that are independent of any I/O framework, database, or web service.
"""
