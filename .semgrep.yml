rules:
  - id: python.eval-exec
    languages: [python]
    message: "Avoid using eval/exec; prefer safer alternatives"
    severity: ERROR
    patterns:
      - pattern: eval($EXPR)
      - pattern: exec($EXPR)

  - id: python.subprocess-shell
    languages: [python]
    message: "Avoid passing shell=True to subprocess calls"
    severity: WARNING
    patterns:
      - pattern: subprocess.run($ARGS, shell=True)
      - pattern: subprocess.Popen($ARGS, shell=True)

  - id: python.print-warning-error
    languages: [python]
    message: "Avoid using print(); use the logging module instead (logger.info/warning/error)."
    severity: ERROR
    patterns:
      - pattern: print(...)
      - pattern: print()
